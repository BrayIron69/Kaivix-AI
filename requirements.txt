fastapi>=0.110.0
uvicorn>=0.29.0
groq>=0.5.0
python-dotenv>=1.0.0
pydantic[email]>=2.0.0
PyYAML>=6.0
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.126.0

# Used directly by scheduling/render_env_sync.py to call Render's own
# API (persisting the calendar OAuth refresh_token so it survives a
# redeploy -- see that module's docstring). Already pulled in
# transitively by google-auth's own transport layer, but declared here
# explicitly rather than relied on implicitly: a future change to
# google-auth's dependencies must not silently remove it.
requests>=2.31.0

# Nothing imports "tzdata" by name -- stdlib zoneinfo reads it. Required
# wherever the OS ships no IANA tz database (Windows has none: TZPATH is
# empty), or ZoneInfo() raises ZoneInfoNotFoundError. Needed by
# scheduling/google_calendar_provider.py in production, not just tests.
tzdata>=2024.1
